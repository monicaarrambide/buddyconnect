<nav class="navbar navbar-expand-lg navbar-dark bg-primary"> 
  <div class="container-fluid" id="navbarText">
    
      
      <ul class="navbar-nav">

        <li class="nav-item active">
          <%= link_to 'Profile', user_path(current_user.studentId), class:"text-light btn btn-primary btn-lg btn-block" %>
        </li>

        <% if current_user.groupId.present? %>
          <li class="nav-item">
            <%= link_to 'Group', group_path(current_user.groupId), class:"text-light btn btn-primary btn-lg btn-block" %>
          </li> 
        <% end %>

        <li class="nav-item">
          <%= link_to 'Forum', posts_path, class:"text-light btn btn-primary btn-lg btn-block" %>
        </li>

        <% if current_user.isAdmin %>
          <li class="nav-item">
            <%= link_to 'Users', users_path, class:"text-light btn btn-primary btn-lg btn-block" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Officers', users_path, class:"text-light btn btn-primary btn-lg btn-block" %>
          </li>
        <% end %>  

        <% if current_user.isAdmin%>
          <li class="nav-item">
            <%= link_to 'All Groups', groups_path, class:"text-light btn btn-primary btn-lg btn-block" %>
          </li>
        <% end %>  
        
      </ul>
      
    <%= link_to "Sign Out", destroy_user_session_path, class:"text-light btn btn-outline-light"%>
  </div>
</nav>



<table>
  <thead>
    <tr>
      <th>Userid</th>
      <th>Phone</th>
      <th>Nationality</th>
      <th>State</th>
      <th>Community</th>
      <th>Numprereqs</th>
      <th>Scholarship</th>
      <th>Favemoviegenre</th>
      <th>Potentialroles</th>
      <th>Numworkexp</th>
      <th>Usedtech</th>
      <th>Projects</th>
      <th>Extracurric</th>
      <th>Pastworkexp</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interests.each do |interest| %>
      <tr>
        <td><%= interest.userId %></td>
        <td><%= interest.phone %></td>
        <td><%= interest.nationality %></td>
        <td><%= interest.state %></td>
        <td><%= interest.community %></td>
        <td><%= interest.numPrereqs %></td>
        <td><%= interest.scholarship %></td>
        <td><%= interest.faveMovieGenre %></td>
        <td><%= interest.potentialRoles %></td>
        <td><%= interest.numWorkExp %></td>
        <td><%= interest.usedTech %></td>
        <td><%= interest.projects %></td>
        <td><%= interest.extracurric %></td>
        <td><%= interest.pastWorkExp %></td>
        <td><%= link_to 'Show', interest_path(interest.userId) %></td>
        <td><%= link_to 'Edit', edit_interest_path(interest.userId) %></td>
        <td><%= link_to 'Destroy', interest_path(interest.userId), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Interest', new_interest_path %>

